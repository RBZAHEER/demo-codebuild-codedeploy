version: 0.2

phases:
  pre_build:
    commands:
      - echo Pre-build phase
  build:
    commands:
      - echo Build phase
  post_build:
    commands:
      - echo Post-build phase
artifacts:
  files:
    - index.html
    - appspec.yml
    - scripts/install_nginx.sh
    - scripts/deploy_nginx.sh
